# Base URL for media assets (gallery images, etc.).
# Default (when unset) is "/gallery" — served from public/gallery locally.
# For production, point to your CDN, e.g.:
# NUXT_PUBLIC_CDN_BASE=https://cdn.example.com/video-plug
NUXT_PUBLIC_CDN_BASE=/gallery
